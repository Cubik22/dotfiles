#!/bin/sh

# set rust local

lib_home="${XDG_LIB_HOME:-$HOME/.local/lib}"
export RUSTUP_HOME="${RUSTUP_HOME_LOCAL:-$lib_home/rustup}"
export CARGO_HOME="${CARGO_HOME_LOCAL:-$lib_home/cargo}"
unset lib_home
