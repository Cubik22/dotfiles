# wayland
gpu-context=wayland

# performace
profile=gpu-hq
scale=ewa_lanczossharp
cscale=ewa_lanczossharp
video-sync=display-resample
interpolation
tscale=oversample

# better audio quality when sped up
af=scaletempo2

# use alsa for audio
ao=alsa

# use pulse for audio (default)
# ao=pulse

# load all subs containing the media filename
sub-auto=fuzzy

# bold the subtitles to increase readability
sub-bold=yes

# do not exit when video ends
keep-open=yes

# save position on quit
save-position-on-quit

# thumbnail script replace built-in OSC
osc=no

# screenshot template and directory
screenshot-template="%F-[%P]v%#01n"
screenshot-directory="/home/lollo/oth/pictures/screenshots"
